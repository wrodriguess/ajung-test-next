/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['grodbzf5x83i.compat.objectstorage.sa-saopaulo-1.oraclecloud.com'],
  },
}

module.exports = nextConfig
